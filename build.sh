# remove node_modules
# sudo npm install -g asar
# npm install production
# create exe / sh archive which extracts an ASAR to rvplayer/modules/[version]
# on CCI, update remote manifest version# and rollout%
