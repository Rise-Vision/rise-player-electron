#!/bin/bash
pkill -f "$HOME/rvplayer/.*RiseCache"
pkill -f "$HOME/rvplayer/.*chrome"
pkill -f "$HOME/rvplayer/.*RisePlayer"
pkill -f "$HOME/rvplayer/.*Installer/installer"
pkill -f "$HOME/rvplayer/.*watchdog/index.js"
