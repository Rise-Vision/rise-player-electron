args = WScript.Arguments;
dir = "\"%LOCALAPPDATA%\\rvplayer\\VERSION\\Installer\\scripts\\";

command = dir + args(0) + "\"";

for (i = 1; i < args.length; i++) {
   command = command + " " + args(i);
}

shell = WScript.CreateObject("WScript.Shell");
shell.Run(command, 0, true);
